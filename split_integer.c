/*
Split integer

Input: Integer with five digits
Output: The five digits with black space between them
*/
#include <stdio.h>

int main ( void )
{
    int number;
    int d1, d2, d3, d4, d5; //Digits
    
    // Read a integer
    printf("Enter a integer: ");
    scanf("%d", &number);
    printf("The number entered was: %d", number);
    // Split 
}